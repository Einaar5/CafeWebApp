@model List<WebApp1.Models.CategoryMenu>

@{
    ViewData["Title"] = "Category List";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2 class="mb-4">Category List</h2>

<table class="table table-dark table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Description</th>
            <th>Created At</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var category in Model)
        {
            <tr>
                <td>@category.Id</td>
                <td>@category.Name</td>
                <td>@category.Description</td>
                <td>@category.CreatedAt.ToString("g")</td>
                <td>
                    <a href="@Url.Action("CategoryEdit", "Admin", new { id = category.Id })" class="btn btn-warning btn-sm">Edit</a>
                    <a href="@Url.Action("CategoryDelete", "Admin", new { id = category.Id })" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure?');">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<a href="@Url.Action("CategoryAdd", "Admin")" class="btn btn-primary mt-3">Add New Category</a>